import 'package:flutter/material.dart';

class PresentationTabView extends StatelessWidget {
  const PresentationTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Presentation Tab')));
  }
}
